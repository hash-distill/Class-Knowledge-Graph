# Python 3.12 + CUDA 12.x environment
# Use PyTorch CUDA wheels from the official index.
--extra-index-url https://download.pytorch.org/whl/cu128

# ── Core runtime ──────────────────────────────────────────────
numpy>=1.26.4,<2.1
scipy>=1.12.0,<1.15
opencv-python>=4.10.0,<4.12
PyYAML>=6.0.2,<7.0
tqdm>=4.66.5,<5.0
pydantic>=2.8.2,<3.0

# ── Deep learning (GPU) ──────────────────────────────────────
 torch==2.7.1+cu118
 torchvision==0.22.1+cu118
 torchaudio==2.7.1+cu118

# ── Detection / Tracking / Pose (YOLO26) ─────────────────────
ultralytics>=8.3.50

# ── Lightweight RTMPose inference (backup for YOLO26-Pose) ───
rtmlib>=0.1.0

# ── OCR ───────────────────────────────────────────────────────
paddleocr>=2.8.1,<3.0
# PaddlePaddle GPU package compatibility changes by CUDA/Python.
# Install the matching paddlepaddle-gpu wheel from the official Paddle site.

# ── API and integration (for future KG / frontend) ───────────
fastapi>=0.112.2,<1.0
uvicorn>=0.30.6,<1.0

# ── Data analysis and visualization ──────────────────────────
pandas>=2.2.2,<3.0
matplotlib>=3.9.2,<4.0

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0,<9.0
